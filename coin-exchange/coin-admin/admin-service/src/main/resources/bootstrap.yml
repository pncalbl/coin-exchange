spring:
  application:
    name: admin-service
  cloud:
    nacos:  # nacos 地址
      server-addr: nacos-server:8848
      config:
        file-extension: yaml
  profiles:
    active: dev
# 拉取的配置文件的 dataID = admin-service-dev.yaml
